package com.example.metadata.outbound;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest(classes = OutboundApplication.class)
public class OutboundApplicationTest {

  @Test
  public void contextLoads() {
    // This test simply verifies that the Spring Application Context loads without issues.
  }
}